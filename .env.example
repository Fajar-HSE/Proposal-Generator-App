# LLM Proxy — set ONE of these and keep API key server-side
LLM_API_KEY=sk-...            # OpenAI / OpenRouter / Groq key (NEVER commit)
LLM_BASE_URL=https://api.openai.com/v1
# LLM_BASE_URL=https://openrouter.ai/api/v1

# Proxy port
PORT=8787

# Monitoring (optional)
VITE_SENTRY_DSN=
